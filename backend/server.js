/*
 * Backend server bootstrap.
 * Used: express, cors, helmet, morgan, cookie-parser, passport, env/db/session/passport config, API routes, and error middleware.
 * Handled: application startup, database connection, middleware registration, route mounting, and global error handling.
 * Exported: none; started the HTTP server process.
 * Removed for open-weight release.
 */
