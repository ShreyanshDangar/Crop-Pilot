/*
 * Authentication middleware.
 * Used: AppError from the shared error middleware.
 * Handled: session-backed route protection for authenticated users.
 * Exported: requireAuth.
 * Removed for open-weight release.
 */
