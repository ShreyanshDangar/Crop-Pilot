/*
 * Backend error middleware utilities.
 * Used: logger and env.
 * Handled: application error types, async controller wrapping, logging, and Express error responses.
 * Exported: AppError, catchAsync, globalErrorHandler.
 * Removed for open-weight release.
 */
