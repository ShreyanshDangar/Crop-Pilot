/*
 * Anthropic analysis service.
 * Used: Anthropic config, imageProcessor, parseClaudeJson, AppError, logger.
 * Handled: multimodal advisory requests to Claude, response parsing, and API error mapping.
 * Exported: analyzeWithClaude.
 * Removed for open-weight release.
 */
