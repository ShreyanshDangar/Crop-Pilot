/*
 * Authentication service.
 * Used: User model, AppError, emailService.
 * Handled: registration bootstrap, email OTP verification, and credential login checks.
 * Exported: initRegistration, verifyRegistrationOTP, loginUser.
 * Removed for open-weight release.
 */
