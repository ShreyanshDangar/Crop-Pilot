/*
 * Environment loader and normalized runtime config.
 * Used: dotenv and process environment variables shared across the backend.
 * Handled: required variable checks and assembled the exported config object.
 * Exported: env.
 * Removed for open-weight release.
 */
