/*
 * Farm advisory routes.
 * Used: express Router, authentication middleware, upload middleware, phase controllers, sessionController.
 * Handled: phase analysis endpoints plus session list/detail/delete and stats endpoints.
 * Exported: router mounted at /farm.
 * Removed for open-weight release.
 */
