/*
 * Authentication routes.
 * Used: express Router, validation middleware, auth validators, authController.
 * Handled: auth endpoints for register/init, register/verify-otp, login, logout, and me.
 * Exported: router mounted at /auth.
 * Removed for open-weight release.
 */
