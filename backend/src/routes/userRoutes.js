/*
 * User routes.
 * Used: express Router, authentication middleware, validation middleware, user validators, userController.
 * Handled: profile, preferences, and account management endpoints.
 * Exported: router mounted at /user.
 * Removed for open-weight release.
 */
