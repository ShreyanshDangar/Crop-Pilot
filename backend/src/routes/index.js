/*
 * Root API router.
 * Used: express Router plus authRoutes, userRoutes, and farmRoutes.
 * Handled: mounted /auth, /user, /farm, and the API health endpoint.
 * Exported: router mounted at /api.
 * Removed for open-weight release.
 */
