/*
 * Claude response parsing helper.
 * Used: native JSON parsing and string extraction utilities.
 * Handled: extracting structured JSON payloads from model text responses.
 * Exported: parseClaudeJson.
 * Removed for open-weight release.
 */
