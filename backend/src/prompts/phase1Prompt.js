/*
 * Phase 1 system prompt definition.
 * Used: ES module export consumed by the phase 1 controller and Anthropic analysis service.
 * Handled: crop-selection advisory instructions for the model layer.
 * Exported: phase1SystemPrompt.
 * Removed for open-weight release.
 */
