/*
 * User controller.
 * Used: User model, error middleware helpers, logger.
 * Handled: profile retrieval, profile updates, preference updates, and account deletion.
 * Exported: getProfile, updateProfile, updatePreferences, deleteAccount.
 * Removed for open-weight release.
 */
