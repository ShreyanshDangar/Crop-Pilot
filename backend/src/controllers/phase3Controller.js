/*
 * Phase 3 advisory controller.
 * Used: error middleware helpers, anthropicService, phase3 prompt, FarmSession model, logger.
 * Handled: harvest-intelligence analysis requests with uploaded imagery and session persistence.
 * Exported: analyzePhase3.
 * Removed for open-weight release.
 */
