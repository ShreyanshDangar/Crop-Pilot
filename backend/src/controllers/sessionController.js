/*
 * Session controller.
 * Used: mongoose, error middleware helpers, FarmSession model.
 * Handled: advisory session history, single-session retrieval, deletion, and dashboard summary stats.
 * Exported: getSessions, getSession, deleteSession, getStats.
 * Removed for open-weight release.
 */
