/*
 * Authentication controller.
 * Used: error middleware helpers, authService, logger.
 * Handled: registration start, OTP verification, login, logout, and current-session user responses.
 * Exported: registerInit, registerVerifyOTP, login, logout, getMe.
 * Removed for open-weight release.
 */
