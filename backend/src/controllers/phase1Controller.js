/*
 * Phase 1 advisory controller.
 * Used: error middleware helpers, anthropicService, phase1 prompt, FarmSession model, logger.
 * Handled: crop-selection analysis requests and persisted advisory sessions.
 * Exported: analyzePhase1.
 * Removed for open-weight release.
 */
