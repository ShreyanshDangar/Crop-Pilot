/*
 * Progressive Web App — service worker script.
 * Used: Service Worker API, Cache Storage API, and Fetch API.
 * Handled: asset precaching, offline navigation fallback, and cache refresh logic.
 * Removed for open-weight release.
 */
