/*
 * Progressive Web App — install prompt component.
 * Used: React state/effects, framer-motion, localStorage, Button, Modal, and the usePWA hook.
 * Handled: deferred install prompting and iOS add-to-home-screen instructions.
 * Removed for open-weight release.
 */

export function InstallPrompt() {
  return null
}
